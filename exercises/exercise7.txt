What's the major difference between an Array and a Hash?

An array is an ordered list of objects, while a hash is a collection of key value pairs. Arrays are indexed by an integer, while a hash is indexed by it's keys.
